models--Systran--faster-whisper-large-v2
models--Systran--faster-whisper-large-v3
__pycache__
# exclude (large) mp3 files and partials
*.mp3
*.part
*.ytdl
*/20??-??-??_???????????
jotaemei_videos
# exclude experimental/sandbox codes
archive
post.py
mkdirectory.py
directory.json
headshots
clips
*.jpg
*.cache.json
concat.txt
# exclude voice samples for automatic speaker tracking 
# (experimental and big)
voices_folder
# exclude credentials for security
hf_token.txt
hf_token_invalid.txt
openai_key.txt
reddit.json

# refactor planning (item 16)
plan.txt

# credentials (cookies.txt = live YouTube session; never commit)
cookies.txt
credentials/

# local scratch: orphaned experiments, never published (plan.txt 0.6)
scratch/
.claude/
medford_tmp/

# privacy: speaker name -> home address mapping. The generated heatmaps are
# aggregated and published; this raw mapping must never be.
addresses*.json

# Retired per-language transcript copies (2026-09-16).
# googletrans 3.4.0 is broken -- half the languages raise, the other half
# silently return English -- so these became ~20,000 duplicate-English pages
# wearing foreign lang attributes. NOT deleted: still on disk, and in history
# at 01245267f2de2472f2563765052468ad376ca6bc. See the recovery banner at the
# top of plan.txt before restoring any of them.
*.es.html
*.pt.html
*.pt-BR.html
*.zh-cn.html
*.ht.html
*.vi.html
*.km.html
*.ru.html
*.ar.html
*.ko.html

# orphaned legacy stub: no transcript body, dead nav to .srt.*.html
*.en.html

# pipeline crash/restart logs from the .bat wrappers
logs/

# per-word timing sidecars -- decide on full rollout before tracking
*.words.json

# alignment resume checkpoint: transient, deleted once model.pkl is written
aligned.checkpoint.pkl
*.checkpoint.pkl
